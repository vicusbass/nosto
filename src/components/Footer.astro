---
import { Icon } from "astro-icon/components";
---

<footer class="footer-gray">
  <div class="mx-auto px-4 md:px-6 lg:px-[70px] py-2 bg-footer">
    <div
      class="flex flex-col md:flex-row md:justify-between md:items-center gap-2 md:gap-8"
    >
      <div class="py-[52px]">
        <h3 class="text-xl mb-4 text-black">NOSTO</h3>
        <div class="flex flex-col gap-2 text-gray-600">
          <p class="flex items-center gap-2">
            <Icon name="mdi:phone" class="h-4 w-4" />+40 123 456 789
          </p>
          <p class="flex items-center gap-2">
            <Icon name="mdi:email" class="h-4 w-4" />contact@nosto.ro
          </p>
          <p class="flex items-center gap-2">
            <Icon name="mdi:home" class="h-4 w-4" />Adresa
          </p>
        </div>
      </div>

      <div
        class="flex items-center justify-center md:justify-end px-4 md:px-0 mt-0 md:flex-grow"
      >
        <div
          class="flex flex-nowrap gap-4 justify-center md:justify-end w-full overflow-x-auto pb-2"
        >
          <button class="btn-dark hidden md:inline-block">CONTACT</button>
          <button class="btn-dark">VEZI APARTAMENTE</button>
          <button class="btn-dark">SUNĂ ACUM</button>
        </div>
      </div>
    </div>
  </div>
</footer>
